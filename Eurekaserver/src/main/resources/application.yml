spring:
  application:
    name: eurekasever
  security:
    basic:
      enable: true
    user:
      name: user
      password: 1234

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false